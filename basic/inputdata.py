# use of inbuilt function  --- input.
#input() -- treats whatever user types/gives input as a string.
a = input() #4
b = input() #5

print(a+b) #works as string concatenation. results in a string -- 45

''' + works as string concatenation as results in joining the 2 strings and 
not in addition.'''

''' - * / are unknown to string functions, so it results in TypeError '''


